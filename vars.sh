a=10
name=john
name="john A"
#used double quotes above because to declare a variable if we need to use a to z, 0 to 9, underscore,,, apart from those if any special character is used then use double quotes. Since above we have space which is will considered a special character so we used double quotes.